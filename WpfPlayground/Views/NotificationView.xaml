<UserControl x:Class="WpfPlayground.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:WpfPlayground.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="50"
             d:DesignWidth="300">
    <UserControl.Resources>
        <viewModels:NotificationViewModel x:Key="DesignViewModel" />
    </UserControl.Resources>
    <StackPanel d:DataContext="{Binding Source={StaticResource DesignViewModel}}"
                Orientation="Horizontal">
        <Rectangle Width="30"
                   Height="30"
                   Margin="5"
                   Fill="{Binding HeaderBrush}" />
        <StackPanel Orientation="Vertical"
                    Margin="5">
            <TextBlock x:Name="Header"
                       Text="{Binding Header}"
                       FontSize="14" />
            <TextBlock x:Name="Message"
                       Text="{Binding Message}" />
        </StackPanel>
    </StackPanel>
</UserControl>
