<UserControl x:Class="WpfPlayground.Views.AsyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <WrapPanel>
        <WrapPanel.Resources>

        </WrapPanel.Resources>
        <StackPanel Style="{StaticResource WrapStackPanel}">
            <TextBlock Text="Async"
                       Style="{StaticResource HeadingStyle}" />
            <Button x:Name="LongProcessButton"
                    Content="Start Long Process" />
            <ProgressBar x:Name="LongProcessProgressBar"
                         Height="20"
                         Minimum="0"
                         Maximum="100"
                         Value="{Binding LongProcessProgressBarProcess}" />
        </StackPanel>
    </WrapPanel>
</UserControl>
