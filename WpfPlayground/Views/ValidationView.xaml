<UserControl x:Class="WpfPlayground.Views.ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:WpfPlayground.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ValidationViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="600">
    <StackPanel>
        <Button x:Name="ValidationButton" Content="Validation"></Button>
        <ListBox ItemsSource="{Binding Errors}" />
    </StackPanel>
</UserControl>
